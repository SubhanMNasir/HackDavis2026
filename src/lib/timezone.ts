// Wellspring's canonical timezone for all date bucketing
// (Reports preset chips, History "Today/Yesterday", CSV filename dates).
// Wire format remains UTC ISO strings — this is for display + bucketing only.
export const APP_TZ = "America/Los_Angeles" as const;
